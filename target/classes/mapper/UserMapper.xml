<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingchun.travelloan.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.qingchun.travelloan.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.qingchun.travelloan.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, phone, email, role, status)
        VALUES (#{username}, #{password}, #{phone}, #{email}, #{role}, #{status})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT * FROM user WHERE phone = #{phone}
    </select>

    <update id="update" parameterType="com.qingchun.travelloan.entity.User">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM user ORDER BY created_at DESC
    </select>

    <select id="selectByRole" resultMap="BaseResultMap">
        SELECT * FROM user WHERE role = #{role} ORDER BY created_at DESC
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM user
    </select>

</mapper>
